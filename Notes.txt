line 1492

if (dump === 'FunctionExpression'){
            console.warn('getExistingComment() aborted, found FunctionExpression as parent');
            return -1;
        }
        
returning -1 causes us to make a new comment