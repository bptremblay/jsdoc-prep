define({
    name: 'PRIMARY_SEARCH_RESULTS',
    bindings: {
     searchQuery:{
      	direction: 'DOWNSTREAM'
  	  },
       searchResult:{
        direction: 'DOWNSTREAM'
      }
    },
    triggers: {
    }
});
